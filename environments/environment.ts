//all library has exactly the same environment variable to avoid build error
export const environment = {
    production: true,
    apiURL: "https://shorturlproject-b2a1cdab2b68.herokuapp.com/api/"
};
